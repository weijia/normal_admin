============
Installation
============

At the command line::

    $ easy_install normal_admin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv normal_admin
    $ pip install normal_admin